// Definiendo una interfaz para el formulario de registro
export interface formRegister {
    
    password: string;
    nombre: string;
    skillLevel: string;
    weeklyHours: number;
    email: string;
  }